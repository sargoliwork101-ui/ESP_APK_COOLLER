{
  "name": "cooler-esp32-app",
  "version": "1.0.0",
  "description": "اپ اندروید کنترل کولر هوشمند ESP32 (WebView محلی، بدون تغییر ظاهر/منطق)",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
